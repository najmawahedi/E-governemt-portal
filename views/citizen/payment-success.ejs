<h2>Payment Successful! ✅</h2>

<div class="alert alert-success">
  <h4>Thank you for your payment!</h4>
  <p>Your payment has been processed successfully.</p>
</div>

<div class="card">
  <div class="card-body">
    <h5>Payment Details</h5>
    <p><strong>Request ID:</strong> #<%= typeof request_id !== 'undefined' ? request_id : 'N/A' %></p>
    <p><strong>Payment ID:</strong> #<%= typeof payment_id !== 'undefined' ? payment_id : 'N/A' %></p>
    <p><strong>Status:</strong> <span class="badge bg-success">Completed</span></p>
    
    <div class="mt-3">
      <a href="/citizen/track?citizen_id=<%= user.id %>&name=<%= encodeURIComponent(user.name) %>" 
         class="btn btn-primary">
        View Your Requests
      </a>
      <a href="/citizen/dashboard?citizen_id=<%= user.id %>&name=<%= encodeURIComponent(user.name) %>" 
         class="btn btn-secondary">
        Back to Dashboard
      </a>
    </div>
  </div>
</div>